"""
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 PROBLEM: [Problem Name]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 DESCRIPTION:
[Paste problem description here]

📊 EXAMPLES:
Input:  
Output: 
Explanation: 

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"""

# ✅ STEP 1: UNDERSTAND (Don't skip this!)
"""
What are we really asking for?
- 

What are the constraints?
- 

What are the edge cases?
- 
"""

# ✅ STEP 2: BRUTE FORCE (Always start here - even if it's slow!)
def brute_force():
    """
    Approach: 
    Time: O()
    Space: O()
    """
    pass

# ✅ STEP 3: OPTIMIZE (Now make it better!)
def optimized():
    """
    Approach: 
    Time: O()
    Space: O()
    
    Key Insight: 
    """
    pass

# ✅ STEP 4: TEST (Run through examples manually first!)
def test():
    # Test case 1:
    assert optimized() == expected
    
    # Test case 2 (edge case):
    assert optimized() == expected
    
    print("✅ ALL TESTS PASSED! YOU'RE A LEGEND!")

if __name__ == "__main__":
    test()

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎉 VICTORY LOG
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Date solved: 
# Time taken: 
# Difficulty: ⭐⭐⭐
# Mistakes made: 
# What I learned: 
# Pattern identified: 
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 
# 🔥 STREAK: [  ] Day 1  →  Keep going, you got this!
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tips:
use loggings
print statements
typeof check for python
break the code into functions

# Problem: <one-line restatement>
# Constraints: n <= ?, values range ?
# Example: input -> output
# Edge cases: []

def solve(input_data):
    # parse input
    # Example: arr = list(map(int, input().split()))
    
    # 1) Quick check edge cases:
    # if len(arr) == 0: return something
    
    # 2) Plan step as comments:
    # - step 1: ...
    # - step 2: ...
    
    # 3) Implement incrementally:
    # result = ...
    # for x in arr:
    #     ...
    
    return result

if __name__ == "__main__":
    # quick tests:
    tests = [
        # (input, expected)
    ]
    for inp, exp in tests:
        out = solve(inp)
        print("OK" if out == exp else "FAIL", out, exp)
